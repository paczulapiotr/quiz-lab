import styles from "./Tile.module.scss";

type Props = { text: string; className?: string };

const Tile = ({ text }: Props) => {
  return (
    <div>
      <p></p>
    </div>
  );
};

export default Tile;
